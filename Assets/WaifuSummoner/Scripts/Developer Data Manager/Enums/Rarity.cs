using UnityEngine;

namespace WaifuSummoner.Enums
{
    public enum Rarity
    {
        Common,     // Normal cards
        Spicy,      // Rare (R)
        Naughty,    // Super Rare (SR)
        Radiant     // Ultra Rare (UR)
    }
}
